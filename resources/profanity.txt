# SRIP Track 2 — Profanity / slur gate wordlist  (PRD §4.2, Phase 2.2)
#
# ───────────────────────── PLACEHOLDER — NOT YET CURATED ─────────────────────────
# This file is an inert scaffold. The Stage 1 profanity gate currently runs on the
# `better-profanity` DEFAULT built-in list (owner approved "use the current list for
# now"). Nothing below is loaded yet. See openissue.md #3.
#
# When the owner supplies the curated content, Phase 2.2 will load this file and:
#   - add every BLOCK term to the profanity matcher (whole-token, case-insensitive,
#     light leetspeak normalization), and
#   - subtract every ALLOW term so clinical / anatomical vocabulary never trips the
#     gate (a real extenuating-circumstances explanation may legitimately use it).
#
# ───────────────────────────────── FORMAT ───────────────────────────────────────
#   - One term per line. Blank lines and lines starting with `#` are ignored.
#   - Lines beginning with `ALLOW:` are allowlist (medical/anatomical) exemptions.
#   - Every other non-comment line is a BLOCK term (slur / profane exclamation).
#   - Terms are matched as whole tokens, case-insensitive. Do not add inflections by
#     hand unless a specific form must be caught; the matcher normalizes light
#     leetspeak (e.g. 0→o, 1→i, 3→e, @→a, $→s).
#
# ============================ BLOCK  (slurs / profanity) =========================
# TODO(owner): primary concern is slurs. Add curated terms below, one per line.
# (Intentionally empty in the placeholder — see openissue.md #3.)


# ====================== ALLOW  (medical / anatomical, never block) ===============
# TODO(owner): clinical/anatomical terms that must NOT be flagged even if a default
# list would catch them. Prefix each with `ALLOW:`. Examples of the intended kind:
# ALLOW: breast
# ALLOW: rectal
# ALLOW: anus
# (Intentionally commented-out in the placeholder — see openissue.md #3.)
