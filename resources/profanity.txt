# SRIP Track 2 — Profanity / slur gate wordlist  (PRD §4.2, Phase 2.2)
#
# LOADED LIVE by the Stage 1 profanity gate (src/srip_filter/gates/essays.py):
#   matcher = better-profanity DEFAULT list  +  BLOCK terms below  −  ALLOW terms below.
#
# ───────────────────────────────── FORMAT ───────────────────────────────────────
#   - One term per line. Blank lines and lines starting with `#` are ignored.
#   - Lines beginning with `ALLOW:` are allowlist exemptions — removed from the
#     matcher even if the default list contains them.
#   - Every other non-comment line is a BLOCK term (slur / profane exclamation).
#   - Terms are matched as whole tokens, case-insensitive. Do not add inflections by
#     hand unless a specific form must be caught; the matcher normalizes light
#     leetspeak (e.g. 0→o, 1→i, 3→e, @→a, $→s).
#
# ============================ BLOCK  (slurs / profanity) =========================
# Deliberately EMPTY (owner, 2026-07-29). better-profanity's built-in list is the whole
# gate. A 1101-term curated list was tried and reverted: at that breadth the list caught
# ordinary vocabulary a real CS applicant writes ("flange", "slave" in a history or
# primary/replica sense, "niggling", surnames like "Dong"), and precision at that scale is
# not achievable by hand.
#
# The reason an imperfect gate is now tolerable: a profanity hit routes to **NEEDS_REVIEW,
# not REJECTED** (owner, 2026-07-29) — a human confirms every flag, so a false positive
# costs a review rather than an application. Adding BLOCK terms is still safe for the same
# reason; add them here, one per line, if the default list proves too narrow in practice.


# ====================== ALLOW  (medical / clinical / innocuous, never block) =====
# Every term below appeared in a *good-faith* applicant essay in the reference
# dataset and was false-positively flagged by the better-profanity default list
# (e.g. "stroke awareness campaigns", "organ donation", "facial recognition",
# "oral presentation", "sex-based differences in medicine"). Clinical/technical
# vocabulary must never trip the gate (PRD §4.2 medical/anatomical exception).
ALLOW: stroke
ALLOW: strokes
ALLOW: organ
ALLOW: organs
ALLOW: oral
ALLOW: orally
ALLOW: facial
ALLOW: thrust
ALLOW: thrusts
ALLOW: sex
ALLOW: sexes
ALLOW: sex-based
ALLOW: sex-specific
ALLOW: breast
ALLOW: breasts
ALLOW: prostate
ALLOW: rectal
ALLOW: anus
ALLOW: vaginal
ALLOW: penis
